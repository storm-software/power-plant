# CLI Reference

Power Plant is operated via the CLI by running the `power-plant run` command. To
stop it, press `ctrl-c`. You may need to wait a bit as power-plant completes
cleanup tasks.

However, Power Plant has more commands:

{{#include ./SUMMARY.md}}
